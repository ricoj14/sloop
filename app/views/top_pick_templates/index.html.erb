<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All top picks
    </h1>

    <a href="/top_picks/new" class="btn btn-block btn-outline-secondary">
      Add a new top pick
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Venue
        </th>

        <th>
          Dish
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @top_picks.each do |top_pick| %>
      <tr>
        <!-- Display top_pick.id start -->
        <td>
          <%= top_pick.id %>
        </td>
        <!-- Display top_pick.id end -->

        <!-- Display top_pick.venue_id start -->
        <td>
          <%= top_pick.venue.name %>
        </td>
        <!-- Display top_pick.venue_id end -->

        <!-- Display top_pick.dish_id start -->
        <td>
          <%= top_pick.dish.cuisine_id %>
        </td>
        <!-- Display top_pick.dish_id end -->

        <!-- Display top_pick.user_id start -->
        <td>
          <%= top_pick.user_id %>
        </td>
        <!-- Display top_pick.user_id end -->

        <!-- Display top_pick.created_at start -->
        <td>
          <%= time_ago_in_words(top_pick.created_at) %> ago
        </td>
        <!-- Display top_pick.created_at end -->

        <!-- Display top_pick.updated_at start -->
        <td>
          <%= time_ago_in_words(top_pick.updated_at) %> ago
        </td>
        <!-- Display top_pick.updated_at end -->

        <td>
          <a href="/top_picks/<%= top_pick.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
